node_modules/
dist/
build/
*.log
.DS_Store

# configuracion local de Claude Code (rutas de maquina, no aporta al repo)
.claude/

# env
.env
.env.local
*.env

# carpeta de datos sqlite (generada por migraciones, incluye .sqlite/-wal/-shm)
backend/data/

# uploads locales de desarrollo (si se usan antes de tener Cloudinary configurado)
backend/uploads/
